import React from 'react';

import { renderHook } from '@testing-library/react';

import useAuth from '../../hook/useAuth';

describe('useAuth', () => {
  //   const settingTestHook = (option: null | boolean, accessToken: string) => {
  //     const { result } = renderHook(() => useAuth(option));
  //   };
});
